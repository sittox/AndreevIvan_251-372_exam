# Лабораторная работа / Экзамен

**Группа:** 251-372
**ФИО:** Андреев Иван Алексеевич

## Задание

Все три вопроса теста являются единым заданием.

**Вопрос 1.**

1. В качестве исходного образа взять alpine:3.14
1. Добавить метку (LABEL description=“QT TCP Server Container”)
1. Установить Qt5 и компилятор (qt5-qtbase-dev, g++)
1. Скопировать файлы проекта в контейнер в папку /home/server
1. Скомпилировать проект в файл с именем qt_tcp_server
1. Открыть порт 5555 и запускать сервер автоматически
1. Собрать образ и разместить на DockerHub в открытом доступе
1. Ссылку на образ прикрепить в Git в виде текстового файла
1. Написать команды, которыми можно собрать образ из dockerfile и запустить
1. Сделать скриншоты всего экрана компьютера при консольной работе по сборке образа и запуске контейнера

**Вопрос 2.**
На основе проекта EchoServer написать проект многопользовательского сервера, который при подключении очередного клиента сервер пишет приветствие.
Если подключается лишний по количеству клиент, то сервер ему пишет, что занят, и просит подключиться позднее, затем сразу же разрывает соединение.
При подключении очередного клиента сервер дополнительно отправляет всем подключенным клиентам информацию о текущем количестве подключенных клиентов.

Игра «Угадай число» (5 клиентов).
Правила: Сервер загадывает число, клиенты по очереди отправляют догадки. Сервер сообщает, больше или меньше загаданного их вариант.
При окончании игры сервер пишет “Угадано” и разрывает соединение со всеми.

**Вопрос 3.**
Создать свой приватный репозиторий github. В качестве collaborator добавить: <https://github.com/gsvmbox> и <https://github.com/ea-bud>.
Работа с репозиторием осуществляется посредством консольных команд.
Клонировать удалённый репозиторий, в основной ветке создать Readme, создать ветку screens со скриншотами,
вернуться в основную ветку, добавить под отслеживание только файлы проекта (не пересобираемые файлы), закоммитить и отправить в удалённый репозиторий.

## Ссылка на образ Docker

См. файл `dockerlink.txt`.